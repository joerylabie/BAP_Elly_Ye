import 'package:flutter/material.dart';
import '../widgets/card2_widget.dart';

class SettingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Card2Widget(title: 'Kaartje'),
    );
  }
}
